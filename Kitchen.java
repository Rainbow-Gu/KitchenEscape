import java.util.ArrayList;

public class Kitchen {

    ArrayList<Wall> walls;
    String direction;
    Wall front; //which wall the player is currently facing

    public void turn (direction) {
        if (Wall front == && String direction == "right") {
            // then describe wall
        }
    }
}